const express=require('express');const axios=require('axios');const app=express();const PORT=process.env.PORT||3000;const TMDB_API_KEY='cdf9b6a0255cebc133ce4d9aaaee8d6d';app.use(express.static('.'));app.use('/api/tmdb',async(req,res)=>{try{const url=`https://api.themoviedb.org/3${req.path}`;const params={...req.query,api_key:TMDB_API_KEY};const response=await axios.get(url,{params});res.json(response.data);}catch(error){res.status(error.response?.status||500).json({error:error.message});}});app.listen(PORT,()=>{console.log(`Proxy server running on port ${PORT}`);});